# Self-play обучение — как это работает

## Контур обратной связи

```
       история за год (3 инструмента, 1h)
                     │
                     ▼
       ┌──────────────────────────┐
       │ Self-play (бар за баром) │
       │  • виден только прошлый  │
       │    бар на момент ставки  │
       │  • выбран лучший сигнал  │
       │    среди всех инструментов│
       │  • ML-фильтр по модели   │
       └──────────┬───────────────┘
                  │ закрытие по TP/SL/таймаут
                  ▼
       ┌──────────────────────────┐
       │ Lesson (урок)            │
       │  • признаки + новости    │
       │  • PnL                   │
       │  • что повлияло          │
       │  • как надо было         │
       └──────────┬───────────────┘
                  │ дозапись
                  ▼
       models/lessons.jsonl
                  │ раз в день (08:00 МСК)
                  ▼
       ┌──────────────────────────┐
       │ Weekly learner (LightGBM)│
       │  → models/current.pkl    │
       └──────────┬───────────────┘
                  │ подхватывается
                  ▼
       следующий цикл self-play
       уже фильтрует входы
```

## Цикл «недели»

`scripts/learning_week.py` запускается как демон/systemd-сервис:

* **08:00 МСК** — тянет свежие бары за 30 дней, прогоняет self-play,
  дополняет `models/lessons.jsonl` и вызывает `train_weekly(...)`.
* **09:00 МСК** — шлёт утренний отчёт о виртуальном счёте (2000 ₽)
  админам в Telegram: `scripts/morning_report.py`.

Накопительно за 5–7 дней модель выходит на 2–5 тыс. уроков и уже
осмысленно отсеивает убыточные сетапы. Первое обучение включается
после 200 уроков порогом `--min-samples` (по умолчанию 200).

## Ручной запуск

```bash
# 1. Self-play на офлайн-истории (для отладки):
python scripts/self_play.py --offline-candles 4000 --target-trades 1000

# 2. Дообучение модели по собранным урокам:
python scripts/train_weekly.py --min-samples 200

# 3. Утренний отчёт:
python scripts/morning_report.py

# 4. Self-play на реальном годе OKX (публичный API, без ключей):
python scripts/self_play.py --days 365 --target-trades 3000
```

## Что считается прибылью

* Виртуальный счёт — 2000 ₽.
* Одна позиция — 5% (100 ₽) от начального капитала, размер не растёт
  после серии убытков, поэтому 100 стопов подряд не обнуляют счёт.
* Комиссия — 0.05% от номинала (имитация maker/taker микса).
* `unrealized_pnl` для long = `(current − entry) × qty`, для short
  наоборот.
* `realized_pnl` фиксируется при закрытии и прибавляется к капиталу.
* Профит-фактор = `Σ положительных / |Σ отрицательных|`.
* Win-rate = доля прибыльных сделок.
* Sharpe и max-drawdown считаются по PnL/эквити.

## Что такое «урок»

Каждая закрытая ставка (`Lesson`) сохраняет:

* признаки на момент входа (ATR%, RSI, объём, тренд, кросс-доходности
  BTC/ETH/SOL за последний час);
* флаг `news_impulse` — был ли перед входом аномальный бар на
  всплеске объёма;
* `influencing_factor` — что более всего повлияло на исход
  (`ATR_SPIKE`, `VOLUME_SURGE`, `RSI_OVERBOUGHT`, `BTC_MOVED_MARKET`,
  `TREND_CONFIRMED`, `NO_STRONG_CATALYST`);
* `counterfactual` — как следовало бы поставить с точки зрения
  пост-фактума;
* `recommendation` — короткий тег для модели
  (`HOLD_WINNER`, `SKIP_HIGH_VOLATILITY`, `WIDEN_STOP_LOSS`...).

Эти поля потом попадают в тренировочный датасет LightGBM.
